using System;
namespace leetcode
{
    class Program
    {
        static void Main(string[] args)
        {
            //two sum
            /* int[] ex1={2,7,11,15};      
            int[] result = TwoSum.twosum(ex1, 9); 
            int[] ex1={3,2,4};      
            int[] result = TwoSum.twosum(ex1, 6);
            Console.WriteLine($"Reuslt={string.Join(", ", result)}");*/

            //add two nums
           /*  ListNode l1 = new ListNode(2);
            l1.next = new ListNode(4);
            l1.next.next = new ListNode(3);

            ListNode l2 = new ListNode(5);
            l2.next = new ListNode(6);
            l2.next.next = new ListNode(4);

            ListNode result = Addtwonums.addTwoNumbers(l1, l2);
            while (result != null)
            {
                Console.Write(result.val);
                if (result.next != null)
                    Console.Write(" -> ");
                result = result.next;
            } */
            
            //LengthOfLongest
           /*  string s = "pwwkew";
            int result = LengthOfLongest.LengthOfLongestSubstring(s);
            Console.WriteLine($"Reuslt={result}"); */
        }
    }
}
